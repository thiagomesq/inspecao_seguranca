import 'package:flutter/material.dart';

class ISImage extends AssetImage {
  final String image;
  const ISImage(this.image) : super(image);

  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
